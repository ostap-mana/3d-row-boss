# ELEMENTAL SIEGE — Playable Ad Spec

**Версія:** 1.0
**Формат:** HTML5 playable ad, один інлайн-файл
**Стек:** PixiJS v8 + TypeScript + Vite + `vite-plugin-singlefile`
**Тривалість:** 22–26 с геймплею → end card
**Орієнтація:** portrait + landscape (обидві обов'язкові)
**Базовий документ:** `elemental-siege-GDD.md`

---

## 1. Головний принцип

> Плеєбл — це **не демка гри**. Це найсмачніші 20 секунд, вирвані з контексту і склеєні так, щоб гравець відчув перемогу до того, як встигне подумати.

З GDD сюди їде **тільки** ланцюжок `матч → комбо → ульта → бос вибухає`. Усе інше — фази боса, Team HP, Obsidian, воскресіння, мета-прогресія — **вирізається повністю**.

### Три правила, які тут головніші за геймдизайн

1. **Гравець не має програти.** Ніколи. Немає фейлу, немає ліміту ходів, немає шкоди по команді.
2. **Гравець не має застрягти.** Якщо він 2.5 с нічого не робить — рука-підказка пульсує голосніше. На 6 с — автохід.
3. **Темп фіксований, а не обрахований.** Див. розділ 4 — це найважливіше технічне рішення в документі.

---

## 2. Технічні обмеження

| Параметр | Значення |
|---|---|
| Розмір файлу | **≤ 5 МБ**, ціль **≤ 1 МБ** |
| Формат | Один `index.html`, усе інлайн (JS, CSS, ассети в base64) |
| Зовнішні запити | **Заборонені повністю.** Жодного CDN, шрифту з Google Fonts, аналітики |
| Аудіо | **Немає.** Автоплей заблокований, звук — це лише вага |
| Мережеві API | MRAID 2.0/3.0 + вендорні обгортки (розділ 8) |
| Час до інтерактиву | < 1.5 с |
| Цільові пристрої | iPhone SE (найгірший кейс) і вище |

### Бюджет ваги

| Компонент | Розмір | Нотатки |
|---|---|---|
| PixiJS v8 (tree-shaken) | ~340 КБ | Без Sound, Accessibility, Filters |
| Код гри | ~45 КБ | |
| Фон арени | ~110 КБ | 1920×1080 WebP q76 → base64 (+37%) = ~151 КБ |
| Арт боса | **0 КБ** | Ріг з `Graphics`, анімується по частинах |
| Портрети героїв ×5 | **0 КБ** | Запікаються в текстури на буті |
| Геми ×5 | **0 КБ** | Малюються через `Graphics` — див. розділ 7 |
| Шрифт | **0 КБ** | Системний стек, без веб-шрифтів |
| **Разом** | **~714 КБ** | Заміряно на `dist/index.html`. Запас ×7 до ліміту |

> Гарний плеєбл важить менше мегабайта. Кожні зайві 500 КБ — це відсотки втраченого показу на слабкій мережі.

---

## 3. Розкадровка по секундах

| Час | Що на екрані | Що робить гравець | Чого його вчать |
|---|---|---|---|
| **0.0–1.2** | Бос вилазить з лави, рев, екран трясе. Дошка з'їжджає знизу | — | «тут великий монстр» |
| **1.2–2.0** | Рука-підказка на потрібному свапі. Текст `MATCH TO ATTACK` | дивиться | правило |
| **2.0–5.0** | **Хід 1.** Свап → 3 водяні геми → промінь у боса → HP 100%→70% | свайп | матч = шкода |
| **5.0–10.0** | **Хід 2.** Свап → каскад → `COMBO x3` спалахує → HP 70%→45% | свайп | каскади дають більше |
| **10.0–16.0** | **Хід 3.** Картка Nyx спалахує `READY`, рука тицяє в неї. Cut-in портрета, повноекранний VFX. HP 45%→20% | тап по картці | **герої — це сила** |
| **16.0–22.0** | **Хід 4.** Бос реве, червоніє. Свап → `COMBO x5` → HP 20%→0% | свайп | фінальний вибух |
| **22.0–24.5** | Бос розсипається, спалах у біле, `VICTORY` | — | нагорода |
| **24.5+** | **End card**: лого, `COLLECT YOUR HEROES`, кнопка CTA | тап → стор | конверсія |

**Хардкап:** якщо гравець дійшов до 30 с і ще не добив — end card вилазить примусово.

---

## 4. Скриптований темп (ключове рішення)

**HP боса не обраховується формулою з GDD.** Воно **прописане** на кожен хід:

```ts
const HP_SCRIPT = [1.00, 0.70, 0.45, 0.20, 0.00];
// індекс = номер зробленого ходу
```

### Навіщо

Гравець зробить не той свап, який ти запланував. Завжди. Якщо HP рахувати чесно — у половини гравців бос на 24-й секунді буде на 60% HP, і креатив помре без кульмінації.

Зі скриптом: **будь-який валідний матч рухає сюжет на один крок.** Гравець відчуває свободу, а ти контролюєш темп до кадру.

Цифри шкоди, що вилітають (`+2,150,000`), рахуються **у зворотний бік** — від різниці в скрипті, а не від ATK героя. Гравець ніколи не помітить.

### Рейки: м'які, не жорсткі

| Підхід | Опис | Вердикт |
|---|---|---|
| Жорсткі | Валідний тільки один запланований свап, решта відскакує | ❌ Відчувається як зламана гра |
| **М'які** | **Будь-який валідний матч приймається, дошка підкладена так, що очевидний — потрібний** | ✅ **Беремо** |
| Без рейок | Чесна гра | ❌ Гравець застрягне |

---

## 5. Скрипт дошки

Дошка **5×5**. Стартова розкладка захардкоджена, черга падіння гемів — теж. Ніякого рандому в креативі.

`F` Fire · `W` Water · `N` Nature · `L` Lightning · `A` Arcane

### Хід 1 — навчальний, водяна трійка

```
      c0 c1 c2 c3 c4
 r0 |  F  N  L  A  F
 r1 |  N  A  F  L  N
 r2 |  W  W  A  F  L     ← рука показує: A(r2,c2) ↕ W(r3,c2)
 r3 |  F  L  W  N  A
 r4 |  N  F  L  A  F
```

Після свапу `r2 = W W W F L` → трійка Water. Каскаду **немає** — перший хід має бути максимально читабельним.

### Хід 2 — каскад до COMBO x3

Черга падіння підкладена так, щоб після очищення утворилось ще два матчі поспіль.

**Інваріант для QA:** `combo === 3` рівно. Не 2, не 5.
Розробник підганяє це редагуванням `DROP_QUEUE`, а не рандомом. Тест має падати, якщо інваріант зламався.

### Хід 3 — ульта Nyx

- Заряд Nyx примусово ставиться на `READY` **після ходу 2**, незалежно від того, скільки водяних гемів реально зібрано.
- Дошка на цьому кроці **не приймає свапи** 0.4 с — щоб гравець помітив картку, що спалахнула.
- Рука-підказка переїжджає з дошки на картку героя.

### Хід 4 — фінал, COMBO x5

Підкладена дошка з великим ланцюгом. `combo` форсується до 5 незалежно від реального результату — це кульмінація, вона не може не спрацювати.

---

## 5а. Хід боса (контратака)

Бос, який стоїть і приймає промені, читається як тренувальний манекен. Після кожного ходу гравця він **б'є у відповідь** — і саме цим ударом кладе обсидіан на дошку.

| Після ходу | Атака | Кому | Шкода |
|---|---|---|---|
| 1 | `breath` — конус лави на весь ряд героїв, з нього ж капають глиби обсидіану | усім | 22% HP |
| 2 | `smash` — обидва кулаки в підлогу, ударна хвиля котиться в героїв, обсидіан вибухає знизу | Nyx (решті — рикошет) | 46% / 15% |
| 3 (ульта) | — | — | — |
| 4 | — | — | — |

**Одна дія — два наслідки.** Плювок лави й атака по героях свідомо злиті в один такт: розіграні окремо, вони коштували б ~3 с, протягом яких гравець просто дивиться.

### HP героїв — теж прописане, не обраховане

- `HERO_MAX_HP` існує лише щоб числа `-1,760` виглядали як числа. Формул немає.
- `HERO_HP_FLOOR = 0.12` — **пати не буває ніколи.** Найгірший випадок лишає смужку червоного. Плейбл, який можна програти — це злитий показ.
- Nyx б'є `smash` саме тому, що герой на межі смерті — набагато сильніший привід тапнути картку, ніж напис «TAP NYX».
- Ульта Nyx лікує весь загін до повного. Це і є відповідь на питання «навіщо мені ці герої».
- Опустіть `HERO_HP_FLOOR` до 0 — і герої справді падають (`HeroCard.down()` живий і робочий). Для A/B, не для продакшену.

---

## 6. Підказки й антизастрягання

| Тригер | Реакція |
|---|---|
| 0.8 с бездіяльності | Рука з'являється, показує свап (луп 1.2 с) |
| 2.5 с | Рука пульсує ×1.3, потрібні геми підсвічуються |
| 6.0 с | **Автохід** — гра робить свап сама, плавно |
| Невалідний свап | М'який відскок, рука одразу показує правильний. **Без звуку помилки, без червоного** |
| Тап повз дошку | Ігнорується (не рахувати як клік по CTA — це фрод) |

---

## 7. Арт-стратегія

### Геми малюються кодом

П'ять гемів через `PIXI.Graphics` — це **нуль кілобайт** і чіткість на будь-якому DPI:

| Стихія | Форма | Колір |
|---|---|---|
| Fire | ромб з зазубреним верхом | `#FF5A1F` |
| Water | крапля | `#2FA8FF` |
| Nature | лист | `#3FD16A` |
| Lightning | блискавка | `#FFD22E` |
| Arcane | шестигранний кристал | `#A855F7` |

**Різні форми обов'язкові** — 8% чоловіків мають дальтонізм, а креатив крутиться саме на них.

### Растром — тільки фон

Бос і портрети героїв виявились дешевшими кодом, ніж бітмапами, тож растр у креативі рівно один — намальована арена (`src/assets/background.webp`).

Картина 16:9 на телефоні 9:16 — це переважно кроп, тому важливий не фіт, а **якір**: зображення накривається по `cover` з невеликим оверскеном, а потім зсувається так, щоб його лінія берега лягла точно на `layout.boss.floor` — ту саму лінію, по якій маскується ріг боса. Помилишся тут — і бос або висить у повітрі, або стоїть по пояс у камені.

Під берегом лежить градієнтний scrim: намальоване плато місцями яскраве, а на ньому стоїть дошка. Це різниця між читабельною дошкою і гарним скріншотом.

Якщо WebP не декодувався (старі WebView) — лишається градієнтне небо. Плейбл, який показує менше, кращий за плейбл, який показує чорний екран.

### Що викинути з референс-екрана

- Псевдо-3D нахил дошки → **пласка дошка**. На 4-дюймовому екрані нахил ламає читабельність
- Дрібні рунічні деталі фону → з'їдають вагу, не читаються
- Текст `7,500,000 / 10,000,000` → **тільки смуга**, без цифр. Ніхто не читає цифри за 20 секунд

---

## 8. Інтеграція з мережами

Один білд, детект мережі в рантаймі:

```ts
const STORE_URL = {
  ios: 'https://apps.apple.com/app/id0000000000',
  android: 'https://play.google.com/store/apps/details?id=com.studio.siege'
};

function onCtaClick(): void {
  const url = /iPhone|iPad|iPod/i.test(navigator.userAgent)
    ? STORE_URL.ios : STORE_URL.android;

  if (typeof (window as any).mraid !== 'undefined') {
    (window as any).mraid.open(url);                      // MRAID: AppLovin, Unity, ironSource
  } else if (typeof (window as any).FbPlayableAd !== 'undefined') {
    (window as any).FbPlayableAd.onCTAClick();            // Meta
  } else if (typeof (window as any).ExitApi !== 'undefined') {
    (window as any).ExitApi.exit();                       // Unity / ironSource legacy
  } else if (typeof (window as any).install === 'function') {
    (window as any).install();                            // Mintegral
  } else if (typeof (window as any).gameEnd === 'function') {
    (window as any).gameEnd();                            // Vungle
  } else {
    window.open(url, '_blank');                           // Preview / fallback
  }
}
```

**Meta додатково вимагає** сигнал готовності:
```ts
window.addEventListener('load', () => {
  (window as any).FbPlayableAd?.onReady?.();
});
```

### Точки CTA

CTA має бути **не одна**. Мінімум три:
1. Кнопка на end card (основна)
2. Постійний банер `INSTALL` угорі з 12-ї секунди
3. Тап по будь-де на end card

---

## 9. Збірка

```bash
npm create pixi.js@latest playable-siege -- --template bundler-vite
cd playable-siege
npm i -D vite-plugin-singlefile
```

`vite.config.ts`:
```ts
import { defineConfig } from 'vite';
import { viteSingleFile } from 'vite-plugin-singlefile';

export default defineConfig({
  plugins: [viteSingleFile()],
  build: {
    target: 'es2015',            // старі WebView в рекламних слотах
    assetsInlineLimit: 100000000, // інлайнити геть усе
    cssCodeSplit: false,
    minify: 'terser',
    terserOptions: { compress: { drop_console: true, passes: 3 } }
  }
});
```

**Альтернатива, яку варто розглянути:** `@smoud/playable-scripts` — збирає окремі білди під кожну мережу однією командою і сам ставить потрібні обгортки. Якщо креативів буде більше одного — окупиться швидко.

---

## 10. QA-чекліст перед здачею

- [ ] Розмір `index.html` < 5 МБ (перевірити **розпакований**, не zip)
- [ ] У файлі **нема жодного** `http://` чи `https://`, крім URL сторів — `grep -o 'https\?://[^"]*' index.html`
- [ ] Працює в **офлайні** (вимкнути мережу, відкрити файл)
- [ ] Portrait і landscape обидва не ламаються
- [ ] iPhone SE (375×667) — дошка й картки не обрізані
- [ ] CTA стріляє з усіх трьох точок
- [ ] Проходиться до кінця без жодного тапу (автохід працює)
- [ ] Проходиться при хаотичному тиканні по всьому екрану
- [ ] Немає `console.log` у продакшн-білді
- [ ] Прогнати через превью-тули: AppLovin, Mintegral, Unity, Meta

---

## 11. Варіанти під A/B

Плеєбл ніколи не їде в одному екземплярі. Мінімальний набір на тест:

| Варіант | Гіпотеза | Що змінюємо |
|---|---|---|
| **A — Baseline** | — | Як у цьому доку |
| **B — Ульта першою** | Сила героя чіпляє сильніше за пазл | Ход 1 = одразу ультимейт |
| **C — Майже програв** | Драма утримує довше | Бос доходить до 5% HP гравця, потім камбек |
| **D — Без туторіалу** | Рука дратує досвідчених | Прибрати підказку до 2.5 с |
| **E — Швидкий** | Коротше = вища конверсія | Стиснути до 3 ходів / 15 с |

**Метрика рішення:** не CTR, а **IPM** (installs per mille) і D1-retention установок. Креатив з високим CTR і поганим ретеншеном — це креатив, що бреше про гру.

---

## 12. Чого тут свідомо немає

| Викинуто | Чому |
|---|---|
| Team HP, поразка | Гравець не має програвати |
| Фази боса, 6 здібностей | Немає часу їх показати |
| Obsidian / Ember / Sealed | Ускладнення без нагороди за 20 с |
| Спеціальні геми | Плутають — гравець не встигає зрозуміти правило |
| 5 героїв в дії | Показуємо **одного** (Nyx). Решта — статичні картки як обіцянка |
| Звук | Заблокований автоплеєм, тільки вага |
| Мета-прогресія | Це продає стор-сторінка, не креатив |

---

*Кінець документа v1.0*
